"use client"

import React from 'react';
import ListItem from '../molecules/list.item';

const test2 = () => {

  const getProduct = async (title: string, options: any) => {
    const queryKey = `https://fakestoreapi.com`;
    const queryFn = await fetch
  }

  return (
    <div>
      {
          
        
        <ListItem title={""} subTitle={""} />
      }
    </div>
  )
}

export default test2